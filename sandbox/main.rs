extern crate sde_specs;
extern crate sde_frames;

// macro_rules! bdd_statement {
//     { Given $input:expr } => { $input };
//     ( When $input:expr ) => { $input };
//     ( Then $input:expr ) => { $input };
// }

// macro_rules! bdd {
//     ($($x: expr),*) => {{
//         let mut vector = Vec::new();
//         $(vector.push($x);)*
//         vector
//     }}
// }

fn main() {
    // trace_macros!(true);

    // let x = bdd_statement! {
    //     When 1 + 1
    // };
    // Given Alice
    // When Alice sign in
    // Then OK
    // Given Alice
    // When Bob sign in
    // Then not found

    // println!("{}", x);

    // trace_macros!(false);
}
